# yukichan-bot-v2

基于 NoneBot2 重建的 yukichan-bot 新版本。

## 框架要求

- 机器人框架使用 NoneBot2。
- 主要支持 OneBot 协议，面向 LLOneBot 等 OneBot V11 实现。
- 兼容 QQ 官方机器人（通过 NoneBot2 的 QQ 官方适配器接入）。
- 两类适配器复用同一个业务插件，避免按平台复制匹配逻辑。

## 已迁移插件列表

| 插件 | 触发指令 / 条件 | 功能说明 |
| --- | --- | --- |
| `match` | `老婆` | 精确匹配回复 |
| `chess` | `国际象棋` / `对弈` / `认输` 等 | 国际象棋对弈插件 |
| `tarot` | `运势预测` / `塔罗占卜` / `抽塔罗牌` / `塔罗` | 塔罗牌占卜 |
| `fadian` | `每日发癫 [名字]` / `小作文` / `发大病` | 随机发癫文案生成 |
| `fortune` | `求签 <事项>` | 日期与用户 ID 哈希算求签运势 |
| `random` | `掷硬币` (`/coin`) / `只因币` / `掷骰子` (`/dice`) / `抽扑克` (`/card`) | 随机掷硬币、骰子、扑克牌及音频 |
| `read60s` | `今日新闻` / `早报` / `60s` | 获取 60s 读懂世界新闻早报图片 |
| `suangua` | `算卦 [事项]` | 周易 64 卦算卦（支持图片与卦象释义） |

## 验证标准

1. 运行 `uv run pytest`，所有插件的单元测试均能够 100% 成功通过。
2. 在 OneBot V11 和 QQ 适配器下均能正常响应上述指令。
