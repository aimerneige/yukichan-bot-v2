# yukichan-bot-v2

基于 NoneBot2 重建的 yukichan-bot 新版本。

## 当前目标

先完成一个可运行的最小 Demo，用于验证新版机器人框架和插件迁移方式。

- 机器人框架使用 NoneBot2。
- 主要支持 OneBot 协议，面向 LLOneBot 等 OneBot V11 实现。
- 尽可能兼容 QQ 官方机器人；这需要通过 NoneBot2 的 QQ 官方适配器接入，不能与 OneBot 共用同一协议适配器。
- 两类适配器应复用同一个业务插件，避免按平台复制匹配逻辑。

## 当前范围

当前版本只实现 `match` 插件中的一条规则：

| 触发条件 | 回复内容 |
| --- | --- |
| 任意消息的纯文本内容精确等于 `老婆` | 肥宅不要乱叫老婆啊！ |

不包含旧项目的其他插件、数据库、管理功能、命令系统或部署配置。

## 验收标准

1. 项目能启动 NoneBot2。
2. 项目同时具备 OneBot V11 与 QQ 官方机器人接入的适配器配置。
3. 在任一已接入平台发送精确文本 `老婆` 时，机器人回复 `肥宅不要乱叫老婆啊！`。
4. 其他文本不触发此规则。
